# Public base URL of your API used by the browser.
# Safe to expose; change locally in .env.local if needed.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Optional: disable Next telemetry locally if you like
# NEXT_TELEMETRY_DISABLED=1
